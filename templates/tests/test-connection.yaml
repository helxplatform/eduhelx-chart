apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eduhelx-chart.fullname" . }}-test-connection"
  labels:
    {{- include "eduhelx-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eduhelx-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
