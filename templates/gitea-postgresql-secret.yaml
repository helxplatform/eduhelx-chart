apiVersion: v1
kind: Secret
metadata:
  name: gitea-postgresql
data:
  {{- if .Release.IsInstall }}
  password: {{ randAlphaNum 50 | b64enc }}
  postgres_password: {{ randAlphaNum 50 | b64enc }}
  {{ else }}
  password: {{ index (lookup "v1" "Secret" .Release.Namespace ( include "gitea.gitea.postgresql.auth.existingSecret" . )).data "password" }}
  postgres_password: {{ index (lookup "v1" "Secret" .Release.Namespace ( include "gitea.gitea.postgresql.auth.existingSecret" . )).data "postgres_password" }}
  {{ end }}