apiVersion: v1
kind: Service
metadata:
  name: {{ include "eduhelx-chart.fullname" . }}
  labels:
    {{- include "eduhelx-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eduhelx-chart.selectorLabels" . | nindent 4 }}
